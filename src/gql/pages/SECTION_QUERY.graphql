query PageFragment ($pageFragmentId: ID!) {
  pageFragment (pageFragmentId: $pageFragmentId) {
    id
    title
    parentKey
    pageId
    key
    language
    wrapper
    data
    creator {
      id
      fullName
    }
    insertedAt
    deletedAt
  }
}
