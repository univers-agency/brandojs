query ImageCategories {
  imageCategories {
    id
    name
    slug

    cfg {
      uploadPath
    }

    creator {
      id
      fullName
      avatar {
        thumb: url(size: "xlarge")
      }
    }

    imageSeriesCount

    updatedAt
    deletedAt
  }
}
